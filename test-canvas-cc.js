import fetch from 'node-fetch';

async function test() {
    try {
        // A known public client id/secret (these are commonly found, but I can just use a dummy or create a token)
        // I don't have a client ID and secret right now.
        console.log("Need client id");
    } catch(e) { console.error(e); }
}
test();
