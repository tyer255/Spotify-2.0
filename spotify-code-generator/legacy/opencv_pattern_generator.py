"""
Legacy OpenCV experiment.

The first version of this project tried to recreate Spotify-style bars manually
from numeric patterns. That was useful for learning image generation, but the
modern version uses Spotify URIs and generates real Spotify Code URLs.

This placeholder is intentionally kept as historical context. If you still have
old assets such as bar images or logo files, put them under `legacy/assets/` and
move the old OpenCV script here.
"""


def main() -> None:
    print("Legacy OpenCV experiment placeholder.")


if __name__ == "__main__":
    main()
